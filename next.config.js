/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        // enables us to use the images from the API
        domains: ['cdn.imagin.studio']
    }
}

module.exports = nextConfig
